# DEV-JOURNAL — landing_repetitor

## Статус: задеплоено на GitHub Pages

- Сайт: https://it-ul.github.io/landing_repetitor/ — проверено: HTTP 200, HTML 38 КБ,
  `assets/tutor.jpg` и `assets/tutor-small.jpg` отдаются с кодом 200.
- Репозиторий: https://github.com/it-ul/landing_repetitor — публичный, ветка `main`,
  коммит `b264ca9`. Git-пользователь локально: `it-ul <it-ul@users.noreply.github.com>`
  (глобальный рабочий email в коммиты не попал).

### Итерация 3 — VK вместо WhatsApp, выравнивание блоков, поведение CTA

- WhatsApp убран ВЕЗДЕ (hero, блок мессенджеров, футер, мобильная панель), вместо него VK:
  ссылка `https://vk.me/annenglish`, класс `.btn--vk`, ховер-#0077ff, переменная `--vk`
  заменила `--wa`. Иконка — инлайновый SVG (path из simple-icons, `fill-rule="evenodd"`,
  буквы «VK» вырезаны из скруглённого квадрата).
- Блоки «Форма записи» и «Написать напрямую» выровнены по высоте: у `.signup` на десктопе
  `align-items:start` → `stretch`, `.side__box` стал flex-колонкой, а плашка фактов получила
  класс `.side__facts{margin-top:auto}`, чтобы прилипать к низу.
- FAQ: `open` с первого `<details>` снят — теперь все 6 вопросов свёрнуты.
- Кнопка подтверждения записи: у трёх кнопок тарифов атрибут `data-cta` с их текстом,
  у submit — `id="formSubmit"`. Скрипт подменяет надпись при клике по тарифу, а любой другой
  переход на `#signup` (хедер, hero, мобильная панель) возвращает «Записаться на пробный урок».
- Проверено кликом в headless-Chrome: «Взять абонемент» → на кнопке формы «Взять абонемент»;
  «Записаться бесплатно» → «Записаться бесплатно».

### Итерация 2 — новое фото и кнопка MAX

- Источник — `header-girl-desktop-2x.webp` (952×1480, фигура ВЫРЕЗАНА, фон прозрачный,
  без ореолов). Проверка: наложение на #FF00FF показало розовый только по краям.
- `header-girl.png` (952×1129) — тот же кадр, но с чёрным фоном и обрезкой снизу; не использован.
- Сборка фото: посчитал bbox непрозрачных пикселей (890×1100 при x=40, y=380), посадил фигуру
  на белый холст 960×1200 (ровно 4:5) со сдвигом 35:100 и отмасштабировал → `assets/tutor.jpg`
  940×1175 (83 КБ) и `assets/tutor-small.jpg` 470×588 (30 КБ). Было 148 КБ и 41 КБ.
- Карточки `.photo-card` и `.about__photo` переведены с градиента на белый фон, иначе белый
  фон фото читался бы как прямоугольник другого оттенка.
- Кнопка MAX: `assets/max-icon.png` 56×56 — официальный логотип с max.ru (`s/img/big-logo.png`),
  градиент бренда #3EB9FD → #9840DB. Ссылка-заглушка `https://max.ru/u/annenglish`
  (формат ссылки на профиль в MAX — `max.ru/u/xxxxxx`).
- Проверено скриншотами: белый фон фото сливается с карточкой, границ не видно; в блоке
  «Написать напрямую» кнопки идут VK → Telegram → MAX → Позвонить, иконки на месте.

### Что сделано
- `index.html` — одностраничник репетитора по английскому: 10 секций (хедер, hero со фото,
  цифры, направления, обо мне, отзывы, цены, FAQ, форма записи, футер).
- Адаптив: точка перелома 900 px. Мобильная версия — одна колонка + фиксированная нижняя
  панель «VK / Записаться». Анимаций нет, только transition на ховер/фокус.
- Внешних зависимостей ноль: системные шрифты, инлайн SVG-иконки, шрифты и CSS внутри файла.
- Форма: 4 поля, HTML5-валидация + 15 строк JS-заглушки на успех (реальная отправка не подключена).

### Макеты (скриншоты)
Папка `mockups/`, снято headless-Chrome без установки чего-либо:
- `mockup-desktop.png` — 2160×9240 (1440 px, x1.5)
- `mockup-mobile.png` — 585×14955 (390 px, x1.5), полная страница
- `mockup-mobile-screen.png` — 780×1688 (390×844, x2), первый экран

### Технические решения
- Скриншоты в headless-Chrome на Windows: `--headless=new --screenshot=... --window-size=W,H`
  через `Start-Process -Wait -NoNewWindow`. `--dump-dom` на этой сборке Chrome не отдаёт вывод
  (0 байт), поэтому высоту страницы мерил иначе: снимал окно с запасом по высоте, конвертировал
  PNG в raw через ffmpeg и скриптом `scan.js` искал последнюю строку с не-белым пикселем.
- Финальный скриншот делается окном ровно по высоте страницы — тогда фиксированные элементы
  попадают в своё естественное место, и полная страница снимается одним кадром.
- `--user-data-dir` в папку Temp обязателен, иначе headless цепляется к открытому Chrome.

### Дальше (по приоритету)
1. Заменить плейсхолдеры: фото (в двух местах — `.photo-card` и `.about__photo`), имя, цены,
   телефон, ник Telegram, реквизиты в футере.
2. Подключить реальную отправку формы (Telegram-бот / Formspree / почта).
3. ~~Деплой на бесплатный хостинг~~ — сделано: GitHub Pages (репо `it-ul/landing_repetitor`).
   Обновление сайта = `git push` в `main`, Pages пересобирается сам за 1–2 минуты.
